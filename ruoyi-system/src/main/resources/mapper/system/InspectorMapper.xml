<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.InspectorMapper">
    
    <resultMap type="Inspector" id="InspectorResult">
        <result property="id"    column="id"    />
        <result property="name"    column="name"    />
        <result property="phone" column="phone"/>
        <result property="password" column="password"/>
    </resultMap>

    <sql id="selectInspectorVo">
        select id, name, phone, password from inspector
    </sql>

    <select id="selectInspectorList" parameterType="Inspector" resultMap="InspectorResult">
        <include refid="selectInspectorVo"/>
        <where>
            <if test="name != null  and name != ''"> and name like concat('%', #{name}, '%')</if>
            <if test="phone != null  and phone != ''"> and phone = #{phone}</if>
        </where>
    </select>
    <!-- 根据手机号查询检测员 -->
    <select id="selectInspectorByPhone" parameterType="String" resultMap="InspectorResult">
        <include refid="selectInspectorVo"/>
        WHERE phone = #{phone}
    </select>
    <select id="selectInspectorById" parameterType="Long" resultMap="InspectorResult">
        <include refid="selectInspectorVo"/>
        where id = #{id}
    </select>

    <insert id="insertInspector" parameterType="Inspector" useGeneratedKeys="true" keyProperty="id">
        insert into inspector
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">name,</if>
            <if test="phone != null and phone != ''">phone,</if>
            <if test="password != null and password != ''">password,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">#{name},</if>
            <if test="phone != null and phone != ''">#{phone},</if>
            <if test="password != null and password != ''">#{password},</if>
        </trim>
    </insert>

    <update id="updateInspector" parameterType="Inspector">
        update inspector
        <trim prefix="SET" suffixOverrides=",">
            <if test="name != null and name != ''">name = #{name},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="password != null and password != ''">password = #{password},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteInspectorById" parameterType="Long">
        delete from inspector where id = #{id}
    </delete>

    <delete id="deleteInspectorByIds" parameterType="String">
        delete from inspector where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>